# Vaani never makes outbound network calls during normal operation.
# This key is ONLY consumed by the demo-video build pipeline (offline narration synth).
# Leave empty for app development.
ELEVENLABS_API_KEY=
